// import u "upstream" exposes T, f, A  // namechecking still missing
// TODO: Add import checking to phase test
module exposes Main

activity Main ()
    // var x: u.T = {x = 42}
    // var y: T = {x = 43}
    await true
end