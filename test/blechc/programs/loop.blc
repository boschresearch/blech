
@[EntryPoint]
activity main ()() 
    var x: int32 = 17
    // while x > 0 repeat
    //     x = x - 1
    //     await true
    // end
    if x > 0 then
        repeat
            x = x - 1
            await true
        until x <= 0 end
    else
        await true
    end
end
