
@[EntryPoint]
activity A()
    var l: float32
    await true
    var r: float32 = 4.7
    l = prev r // local rhs simple direct prev
    await true
    l = prev r // local rhs simple direct prev
    
end
